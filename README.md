# Authorized Security Testing Callback Dashboard

A simple Node.js and Express dashboard for authorized security testing callbacks.

## Local setup

```bash
npm install
npm start
```

Open `http://localhost:3000`.

## Render deployment

- Build command: `npm install`
- Start command: `npm start`

The app listens on `process.env.PORT` or falls back to port `3000` locally.
